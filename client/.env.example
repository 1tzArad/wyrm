# API base URL of the backend.
# Leave empty to send requests to the Vite dev server, which proxies
# /register and /login to http://localhost:8080 (recommended for local dev,
# since the backend does not enable CORS).
# Set to e.g. http://localhost:8080 to call the backend directly.
VITE_API_BASE_URL=
